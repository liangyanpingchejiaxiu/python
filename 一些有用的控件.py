# testcmd = root.register(test)
#validatecommand是validate鼠标事件执行后执行的命令
#validatecommand=(testcmd,'%p') %p是会把输入框的内容传给testcmd指定的函数
#Entry(root,state='readonly'只读)
#def test(content)): if content =='' print return True  else
#  content.isdigit() 是数字
#e=Entry(root) e.grid(row=,column=) 行列
